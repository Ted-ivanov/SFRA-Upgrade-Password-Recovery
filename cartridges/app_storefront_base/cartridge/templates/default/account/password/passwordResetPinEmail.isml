<h1>${Resource.msg('msg.passwordemail.dear', 'login', null)}&nbsp;${pdict.firstName}&nbsp;${pdict.lastName},</h1>

<h1>${pdict.content}</h1>

<h1>Your pin is: ${pdict.pin} and it's valid for ${pdict.time} minutes</h1>

<isprint value="${pdict.emailHeader}" encoding="off"/>